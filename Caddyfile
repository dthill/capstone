:80 {
        encode gzip

        handle /api/* {
                reverse_proxy /api/* backend:8080
        }

        handle {
                root * /var/www
                try_files {path} /index.html
                file_server
        }
}
